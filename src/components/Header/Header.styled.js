import styled from "styled-components";

export const StyledHeader = styled.header`
  display: flex;
  justify-content: space-between;
  min-width: 240px;
  width: 100vw;
  flex: 1;
  padding: clamp(2rem, 5vw, 6rem) clamp(2rem, 5vw, 6rem) clamp(1rem, 3vw, 4rem) clamp(2rem, 5vw, 6rem);
`;
